'use client';

// This component is now obsolete as the booking system has been deprecated.
// It is kept to avoid breaking imports but its functionality is removed.
export function BookingSubmissions() {
    return null;
}
